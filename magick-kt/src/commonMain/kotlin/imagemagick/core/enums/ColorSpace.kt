package imagemagick.core.enums

/**
 * Specifies a kind of color space.
 */
enum class ColorSpace {
    /** Undefined. */
    UNDEFINED,
    /** CMY. */
    CMY,
    /** CMYK. */
    CMYK,
    /** Gray. */
    GRAY,
    /** HCL. */
    HCL,
    /** HCLp. */
    HCLP,
    /** HSB. */
    HSB,
    /** HSI. */
    HSI,
    /** HSL. */
    HSL,
    /** HSV. */
    HSV,
    /** HWB. */
    HWB,
    /** Lab. */
    LAB,
    /** LCH. */
    LCH,
    /** LCHab. */
    LCHAB,
    /** LCHuv. */
    LCHUV,
    /** Log. */
    LOG,
    /** LMS. */
    LMS,
    /** Luv. */
    LUV,
    /** OHTA. */
    OHTA,
    /** Rec601YCbCr. */
    REC601YCBCR,
    /** Rec709YCbCr. */
    REC709YCBCR,
    /** RGB. */
    RGB,
    /** scRGB. */
    SCRGB,
    /** sRGB. */
    SRGB,
    /** Transparent. */
    TRANSPARENT,
    /** XyY. */
    XYY,
    /** XYZ. */
    XYZ,
    /** YCbCr. */
    YCBCR,
    /** YCC. */
    YCC,
    /** YDbDr. */
    YDBDR,
    /** YIQ. */
    YIQ,
    /** YPbPr. */
    YPBPR,
    /** YUV. */
    YUV,
    /** LinearGray. */
    LINEARGRAY,
    /** Jzazbz. */
    JZAZBZ,
}
