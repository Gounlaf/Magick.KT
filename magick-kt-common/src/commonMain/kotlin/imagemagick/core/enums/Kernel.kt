package imagemagick.core.enums

/**
 * Specifies the built-in kernels.
 */
public enum class Kernel {
    /** Undefined. */
    UNDEFINED,

    /** Unity. */
    UNITY,

    /** Gaussian. */
    GAUSSIAN,

    /** DoG. */
    DO_G,

    /** LoG. */
    LO_G,

    /** Blur. */
    BLUR,

    /** Comet. */
    COMET,

    /** Binomial. */
    BINOMIAL,

    /** Laplacian. */
    LAPLACIAN,

    /** Sobel. */
    SOBEL,

    /** Frei chen. */
    FREI_CHEN,

    /** Roberts. */
    ROBERTS,

    /** Prewitt. */
    PREWITT,

    /** Compass. */
    COMPASS,

    /** Kirsch. */
    KIRSCH,

    /** Diamond. */
    DIAMOND,

    /** Square. */
    SQUARE,

    /** Rectangle. */
    RECTANGLE,

    /** Octagon. */
    OCTAGON,

    /** Disk. */
    DISK,

    /** Plus. */
    PLUS,

    /** Cross. */
    CROSS,

    /** Ring. */
    RING,

    /** Peaks. */
    PEAKS,

    /** Edges. */
    EDGES,

    /** Corners. */
    CORNERS,

    /** Diagonals. */
    DIAGONALS,

    /** Line ends. */
    LINE_ENDS,

    /** Line junctions. */
    LINE_JUNCTIONS,

    /** Ridges. */
    RIDGES,

    /** Convex hull. */
    CONVEX_HULL,

    /** Thin SE. */
    THIN_S_E,

    /** Skeleton. */
    SKELETON,

    /** Chebyshev. */
    CHEBYSHEV,

    /** Manhattan. */
    MANHATTAN,

    /** Octagonal. */
    OCTAGONAL,

    /** Euclidean. */
    EUCLIDEAN,

    /** User defined. */
    USER_DEFINED,
}
