package imagemagick.core.enums

//
// /!\ do not modify order: it's directly linked to the underlying C library enum/order /!\
//

/**
 * An enumeration for pixel mapping mode.
 */
public enum class PixelMapping {
    /** RGB. */
    RGB,

    /** BGR. */
    BGR,

    /** RGBA. */
    RGBA,

    /** ABGR. */
    ABGR,

    /** CMYK. */
    CMYK,

    /** BGRA. */
    BGRA,
}
