package imagemagick.core.enums

//
// /!\ do not modify order: it's directly linked to the underlying C library enum/order /!\
//

/**
 * Specifies color transform modes.
 */
public enum class ColorTransformMode {
    /** High resolution (see [Double]). */
    HIGH_RES,

    /** Quantum. */
    QUANTUM,
}
